#!/bin/sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required: install it before committing" >&2
  exit 127
fi

exec gitleaks git --redact=100 --pre-commit --staged .
