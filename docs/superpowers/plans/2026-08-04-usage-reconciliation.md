# Usage Reconciliation — Implementation Plan

1. Specify deterministic reconciliation and mismatch behavior with fixtures.
2. Implement a pure quantity engine with strict month and count validation.
3. Add a tenant-scoped read-only API sourcing operational evidence and labeled invoice lines.
4. Register the protected route and extend security regression coverage.
5. Surface the monthly report and deltas on Billing.
6. Run full domain, API-security, TypeScript, production-build, and hygiene verification before opening the stacked draft PR.
